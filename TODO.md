# TODO

## OPC Infrastructure

Goal: make AiTeam one infrastructure layer for OPC, where one person acts as CEO and uses AI agents to operate project teams.

- Define the OPC operating model: Founder/CEO -> company portfolio -> local repo project -> AI roles -> AiTeam audit -> rule feedback -> next execution loop.
- Add an onboarding preset for a solo founder: one command creates config, starts dashboard, enables Claude Code hooks, and documents Codex sync.
- Provide project templates for `CLAUDE.md`, `项目规则.md`, MVP checklist, QA checklist, and PMO review cadence.
- Add an OPC scorecard: founder leverage, project velocity, capital/token efficiency, role quality, delivery confidence, rule-improvement loop health.
- Turn AiTeam recommendations into safe patch proposals for project `.md` files, with explicit user approval before write.
- Keep all company/project conversation data local by default; any future cloud sync must be opt-in and redactable.
- Package the tool for easy installation through npm or a single binary once the local workflow is stable.
